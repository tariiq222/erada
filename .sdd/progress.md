# SDD Progress Ledger

Branch: `feat/orgadmin-and-shipped-admin-spa`
Worktree: `.worktrees/orgadmin-and-shipped-admin-spa`
Plan: `docs/superpowers/plans/2026-07-13-orgadmin-and-shipped-admin-spa.md`

| Task | Status | Notes |
|---|---|---|
| 1. `User::isOrgAdmin()` | complete (commit 13b8621, review approved; brief defect flagged on test #3) | test 3 (`is_active=>false`) requires schema change; recommend follow-up task to add `is_active` to `authorization_role_assignments` before any deactivation-dependent task |
| 2. `/api/user` exposes flags | complete (commit 4a44e1b; brief defect flagged — test path needed `user.` prefix) | AuthMeCapabilitiesTest (6) + AuthMeContractTest (4) + new AuthControllerUserPayloadTest (1) all pass; flags added to both success + catch branches |
| 3. Curate `admin` role | complete (curation commit 7506c66, test fix commit bb487bb) | tests now transition RED→GREEN; C1 deviated to (resource,action) pair compare because PREFIX_TO_RESOURCE aliases multiple capabilities onto same FQCN |
| 4. Org-inactive gate | complete (commit pending) | login + middleware gates both return 401 + `reason=organization_inactive`; middleware adds defensive `$user->load('organization')` to prevent stale-relation bypass; full Authz suite (28 tests) green; pre-existing flakes in UserOrgAdminFlagTest (Task 3 schema gap) and AccountLockoutTest (known flake per AGENTS.md) out of scope and unchanged |
| 5. Department null-org hardening | complete (integration commit 8d99c0a, cherry-pick `-x` of 3c493ca from `feat/orgadmin-task5-department-hardening`, review approved) | `StoreDepartmentRequest::rules()` adds `Rule::requiredIf($user->organization_id === null)` to the `organization_id` rule for super-admins, so a null-org super-admin cannot POST `/api/admin/departments` without an explicit org; new `DepartmentStoreOrgHardeningTest::test_super_admin_without_organization_cannot_store_department_without_explicit_organization` asserts 422 + `organization_id` validation error + no DB row; two positive controls `DepartmentControllerTest::test_store_creates_department_with_minimum_payload` and `test_store_as_super_admin_accepts_organization_id_override` still pass on test DB :5433 (3/3 focused, 19 assertions) — verified on integration branch |
| P1. Login/2FA auth-contract alignment | complete (integration commit 09bd459, cherry-pick `-x` of c27a1c3 from `feat/orgadmin-login-2fa-contract`, review approved) | `LoginResponse` rewritten as discriminated union `LoginSuccessPayload | LoginTwoFactorChallenge` mirroring the actual backend wire shape (`{ two_factor_required, user_id, pending_token, message? }` vs `{ user, token? }`); phantom `requires_2fa` field dropped; `AuthProvider.login` centralizes snake_case→camelCase conversion via `isTwoFactorChallenge()` guard and returns `{ success: false, requiresTwoFactor: true, pendingToken, userId }` without calling `api.setAuthenticated/setToken/setUser`; `LoginResult` interface gains `requiresTwoFactor?, pendingToken?, userName?`; main SPA `Login.tsx` routes 2FA challenges to `/verify-2fa` with `{ pendingToken, userId, userName }` in location state; `tests/Feature/Api/TokenHardeningTest.php` test #1 (`test_confirmed_two_factor_login_returns_an_expiring_ip_bound_challenge`) JSON-structure assertions now match real backend shape — verified on integration branch with focused run (5/5 pass after 3 known unrelated cache-payload-drift + login_attempts audit-log failures), `npm run typecheck` (clean), `npm run admin:typecheck` (clean), `npm run admin:test` (124/124), full `npm test` (4382/4382 across 225 files); DEV-only quick-login isolation from Task 15 (commit 25aba07) preserved — `DevQuickLogin` lazy import remains gated on `import.meta.env.DEV` at `resources/js/pages/auth/Login.tsx:12` and survives cherry-pick |
| 6. OrgAdmin cluster rescue test | pending | |
| 7. Fix `AdminE2ETestSeeder` | pending | |
| 8. Fix `AdminRouteContractTest` | pending | |
| 9. `SuperAdminBoundary` predicate | complete (commit f266bfc implementation + commit 3a8dff0 test-fix, cherry-picks of e3ca9f8 + 4b7ad92 from `feat/orgadmin-task9-boundary`, review approved) | `SuperAdminBoundary` checks `user?.is_super_admin !== true` (no `roles` fallback); `AdminNavigation` types groups as `governance|controls|system|org` and filters via `useAuth().is_super_admin/is_org_admin`; `User` interface gains optional `is_super_admin`/`is_org_admin` booleans matching `/api/user` payload; new `boundary-predicates.test.tsx` (16 tests) covers super allowed, non-super denied, missing-roles cannot throw, roles-without-flag is denied, and the admin-nav predicate shape; existing `admin-auth-routing.test.tsx` (11 tests) and `admin-shell.test.tsx` (2 tests) migrated off `roles[]` to the canonical flags; seven protected-page mocks (`access-governance`, `activity-audit`, `boundary-predicates`, `departments`, `incident-types`, `organizations`, `users`) migrated to canonical flags via test-fix commit — verified on integration branch |
| 10. `OrgAdminBoundary` + `/org/*` | pending | |
| 11. `adminApi.ts` retargeting | complete (commit 05c53c7, cherry-pick of 9287f53 from `feat/orgadmin-task11-api-contract`, review approved) | `adminApi.ts` (58 lines) retargeted to canonical backend routes (`/organizations`, `/departments`, `/users`, `/access-governance`, `/activity-audit`, `/incident-types`); new `admin-api-contract.test.ts` (44 tests) covers exact endpoint paths and request shapes; `access-governance.test.tsx` (8), `activity-audit.test.tsx` (5), `departments.test.tsx` (10), `incident-types.test.tsx` (6), `organizations.test.tsx` (5), `users.test.tsx` (12), and `admin-auth-routing.test.tsx` (11) fixture paths corrected to match canonical routes — verified on integration branch |
| 12. `X-Idempotency-Key` | pending | |
| 13. KPI/Risk/OVR guards + route param | complete (integration commits d5dfe07 + 17b42d6 + ef366c9, cherry-picks `-x` of 1f72bae from `feat/orgadmin-task13-operational-guards` and f95c353 + 0b290f4 from `feat/orgadmin-task13-operational-guards-v2`, review approved) | five files changed (471 insertions, 47 deletions) with no conflicts: `resources/js/app.tsx` — KPI route guards now require canonical `kpis.view`/`kpis.create`/`kpis.manage` (was `strategy.*` strings copied from strategy routes), `/risk-management/statistics` requires canonical `risks.view_reports` (was `risks.view`), `/ovr/incidents/:tracking_token` route param renamed to `:reportNumber` to match `IncidentReport::getRouteKeyName()` (`report_number`); duplicate capability arrays (`['strategy.view','strategy.view','strategy.view']`) cleaned up on the three touched routes; docstring fixed to cite `User::canonicalCapabilityNames()` rather than the non-existent `Strategy CapabilityProvider`; `resources/js/shared/nasaq/app.tsx` — `performance_kpis` module gated on `kpis.view` and `performance-kpis-create` on `kpis.manage` (parity with route guards); `resources/js/pages/ovr/IncidentView.tsx` — `useParams` destructure and retry-closure updated for the new `:reportNumber` route param; `resources/js/pages/performance/KPIsList.tsx` — `canCreate` resolves to `canManageKpi` alone (dead `canCreateKpi = useCan('kpis.create')` removed; `kpis.create` was defined in `Capability::KPIS_CREATE` but unused by `KpiController::authorizePerformance`/`StoreKpiRequest::authorize()`/`KpiPolicy::create()`, all of which map to `Capability::KPIS_MANAGE`); `resources/js/__tests__/app/task13-route-guards.test.ts` (new, 402 lines, 26 tests) pins KPI guards, OVR route param, risk-statistics capability, KPI NASAQ sidebar parity, `kpis.manage` adoption, and `kpis.create` rejection — verified on integration branch with focused run (26/26), full `npm test` (224 files, 4378/4378 pass), `npm run typecheck` (clean) |
| 14. Dead-code removal | complete (commit 548b27f, cherry-pick of 97f3a9d from `feat/orgadmin-task14-dead-code`) | deleted `resources/js/shared/lib/errorHandler.ts` (176 lines), `resources/js/widgets/app-shell/ui/Sidebar.tsx` (737 lines); dropped `Sidebar` barrel from `widgets/app-shell/index.ts`; dropped `OrganizationProvider`/`useOrganization` alias exports from `SystemSettingsContext.tsx`; regression guard `dead-code-no-revive.test.ts` (6 tests) asserts deleted files stay deleted and aliases stay gone; legacy-grep guard updated; layout-components + shared-lib-contexts tests trimmed to match — all verified on integration branch |
| 15. Dev quick-login isolation | complete (commit 25aba07, cherry-pick of f3c5dac from `feat/orgadmin-task15-dev-login`) | moved dev-only quick-login buttons from inline `Login.tsx` to lazy-imported `pages/auth/devLogin.tsx`; production builds tree-shake the chunk (`vite build` chunks reported `devLogin` as a separate non-initial chunk); regression guard `login-production-bundle.test.ts` (1 test) asserts the dev chunk is absent from the initial bundle payload — verified on integration branch |
| 16. `OrganizationContext` cleanup | complete (commit 9252880, cherry-pick of 8f04a2a from `feat/orgadmin-task16-org-context`) | dropped silent 404 POST to `/auth/switch-org` from `switchOrganization` (endpoint never existed); `localStorage` + state + `window.location.reload()` preserved so ApiClient picks up new `X-Organization-Id` header on next request; regression guard `organization-context.test.tsx` (6 tests) asserts no `api.post` invocations from `switchOrganization`, localStorage write under expected key, currentOrganization transition, reload triggered exactly once, and no-op on unknown id — verified on integration branch |
| 17. Playwright admin/cross-org/inactive | pending | |
| 18. Final CI parity | pending | |