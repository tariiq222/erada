/**
 * Comment entity — model.
 *
 * No standalone types/enums/interfaces were defined in the original
 * `lib/api/comments.ts`; this module exists as the FSD model slot and may be
 * populated as comment domain types are extracted.
 */
export {};
