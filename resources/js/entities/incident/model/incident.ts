/**
 * Incident (OVR) entity — model.
 *
 * No standalone types/enums/interfaces were defined in the original
 * `lib/api/ovr.ts`; this module exists as the FSD model slot and may be
 * populated as incident domain types are extracted.
 */
export {};
