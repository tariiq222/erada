---
context: default
phase: independent-admin-application
task: 9
total_tasks: 11
status: in_progress
last_updated: 2026-07-11T23:55:56.969Z
---

# Independent Admin Application — Complete Handoff

## BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failures. A resuming agent must acknowledge and structurally mitigate them before changing files.

- [ ] SHARED TEST DATABASE: `iradah_pmo_test` on port `5433` is concurrently used by another authorization worktree. Do not run Admin E2E `migrate:fresh` against it while that work is active. Use `ADMIN_E2E_DB_DATABASE=iradah_pmo_admin_e2e_test` locally.
- [ ] HOSTILE DIRTY TREE: the working tree contains extensive unrelated Meetings work, including live changes in `resources/js/app.tsx`. Use exact-path Git operations only. Never stash, clean, reset, checkout, rebase, or stage globally.
- [ ] SERIAL CUTOVER: Task 10 must re-read the current `resources/js/app.tsx` diff immediately before editing and preserve all Meetings permission hunks byte-for-byte.
- [ ] HONEST E2E STATE: `e2e/admin/admin-organizations.spec.ts` is interrupted and incomplete. Do not report Task 9 green until every spec is run separately and the full clean suite passes.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Shared DB destructive setup | Admin E2E `migrate:fresh` deadlocked with and erased fixtures used by concurrent tests on `iradah_pmo_test`. | blocking | Run local Admin E2E only with `ADMIN_E2E_DB_DATABASE=iradah_pmo_admin_e2e_test`; verify the resolved DB before setup. |
| Array sessions in real-browser tests | `SESSION_DRIVER=array` caused CSRF 419 because the Sanctum session did not survive the CSRF-cookie and login requests. | blocking | Keep the Playwright Laravel server on `SESSION_DRIVER=file` and explicitly create `storage/framework/sessions`. |
| Broad `/api` Vite proxy | Vite served `resources/admin/api/adminApi.ts` as `/api/adminApi.ts`, which the old proxy sent to Laravel and prevented the app from booting. | blocking | Retain the extension-only source-module bypass and the architecture contract; never bypass ordinary JSON API paths. |
| Whole-tree staging | The tree contains unrelated Meetings and authorization work. | blocking | Stage/commit only enumerated admin handoff or task files; inspect `git diff --cached` before every commit. |
| Declaring interrupted browser work green | Organization E2E was interrupted while resolving a selector and has no valid pass receipt. | advisory | Resume from the exact selector failure and run the spec to a real persisted create/update/delete result with cleanup. |

<current_state>

Repository: `/Users/tariq/code/erada-platform`

Branch/HEAD: `main` at `4b81476 fix(admin): harden deployment boundary`.

The approved 11-task implementation plan is at Task 9. Tasks 1–8 are committed and independently reviewed. Task 9 is deliberately paused with unstaged work. Tasks 10–11 have not started. There are no Admin E2E Laravel/Vite/Playwright listeners left on ports 8000/4174.

The Admin app is already independently buildable, uses canonical `/api/admin/*` routes, owns login and real 2FA flow, enforces `super_admin`, contains all planned governance/control-plane/user/department/incident surfaces, and has a dedicated hardened Docker/Nginx topology. What remains is complete real-browser proof, operational-router cutover, legacy frontend cleanup, and the final verification/audit matrix.

</current_state>

## Final Objective

Deliver a production-ready admin React application that is independent in frontend build, runtime, deployment, route ownership, quality gates, and browser verification while retaining the existing Laravel backend, PostgreSQL/Redis data plane, Sanctum security model, authorization engine, tenant boundaries, translations, and design system.

The final architecture must prove:

1. `resources/admin/**` has its own Vite entry, TypeScript/test graphs, manifest, commands, Docker image, Nginx runtime, and CI gates.
2. The dedicated admin origin owns every admin route without an `/admin` prefix and supports direct deep-link reload.
3. Same-origin `/api` and `/sanctum` proxying works with HttpOnly cookies, CSRF, secure production cookies, trusted forwarding, and no browser token storage.
4. The operational SPA owns no `/admin/*` route and links eligible super admins to the configured external admin origin.
5. Frontend and backend both restrict the global control plane to literal `super_admin`.
6. Canonical `/api/admin/*` endpoints retain validation, FormRequest authorization, idempotency, throttling, audit behavior, and organization/department isolation.
7. Arabic-first RTL, English parity, theme, responsive navigation, accessibility, loading/empty/error/forbidden/success states all remain proven.
8. Cutover is reversible by routing/link configuration and requires no schema or data rollback.

## Required Final Deliverables

- Independent admin source tree and route inventory under `resources/admin/**`.
- Independent scripts: `admin:dev`, `admin:typecheck`, `admin:lint`, `admin:test`, `admin:build`, and `admin:quality`.
- Forbidden-import boundary enforcement and independent manifest/output proof.
- Canonical control-plane API namespace plus temporary tested compatibility routes.
- Admin-owned login, 2FA challenge, safe return URL, forbidden surface, responsive shell, and full page set.
- Independent `Dockerfile.admin`, Nginx proxy/static configuration, Compose topology, health/readiness and deploy-chain gates.
- Five real Playwright Admin specs covering auth, governance, organizations, access/incident/activity, and users/departments.
- Operational application cutover using a safe centralized `VITE_ADMIN_URL` helper and absolute external links.
- Proven deletion of dead operational admin pages/shell only after repository-wide zero-consumer searches.
- Arabic/English key parity and real-tree RTL tests.
- Full frontend/backend/E2E/static-analysis/container verification receipts.
- Requirement-by-requirement Definition of Done evidence and a fresh independent read-only R3/Terra verdict.

<completed_work>

### Task 1 — Governance contract repair — COMPLETE

Commits `bb8d88b..063264e`; review approved. Fixed overview contract, audit paginator metadata, actor minimization, phantom scope-type creation, and associated tests.

### Task 2 — Canonical Admin API — COMPLETE

Commit `ae301ed`; review approved. Added module-owned `/api/admin/*` aliases with real controller/FormRequest reuse and contract coverage for authn/authz/success/validation/tenant behavior.

### Task 3 — Independent frontend toolchain — COMPLETE

Commits `7431168`, `1beafaa`, `956aecb`; review approved after two remediation cycles. Added independent entry, Vite/TS/Vitest graphs, scripts, lint/design/boundary gates, and import parsing hardening.

### Task 4 — Auth, 2FA, super-admin gate, responsive shell — COMPLETE

Commits `ad1137d`, `e3d42b3`, `2cf44a2`, `1e70edf`; review approved after three security remediation cycles. Added admin-owned auth routes/providers/shell and repaired real two-factor challenge lifecycle.

### Task 5 — Governance pages — COMPLETE

Commit `9cdffa7`; review approved. Migrated overview, security alerts, recent audit, and admin API models/client behavior.

### Task 6 — Control-plane pages — COMPLETE

Commits `217383d`, `304591a`, `1a89117`; review approved after remediation. Migrated organizations, roles, access/governance, scope types, activity logs, scoped-role audit; fixed concurrent governance saves and stale states.

### Task 7 — Users, departments, incident types — COMPLETE

Commits `6760312`, `07f27f8`, `595fad4`; review approved after remediation. Added tenant-safe user/department/incident administration, organization-scoped validation, export search, and async race protection.

### Task 8 — Deployment topology — COMPLETE

Commits `1de9cb6`, `4b81476`; review approved after remediation. Added pinned Admin image, hardened Nginx proxy/static behavior, local topology, production cookie contract, build/deploy gates, and source-map/manifest denial.

### Task 9 — Integration/Admin E2E — IN PROGRESS

Implemented but not committed:

- Dedicated two-server Playwright config: Laravel `127.0.0.1:8000`, Admin Vite `127.0.0.1:4174`, serial browser worker, no server reuse.
- Safe test setup with explicit PostgreSQL environment, `config:clear`, file sessions, runtime session directory creation, `migrate:fresh`, deterministic guarded seeding, and local database overrides.
- Guarded `AdminE2ETestSeeder` requiring `APP_ENV=testing` and a database ending `_test`; fixtures include super admin, confirmed-2FA admin, regular user, two organizations/departments, and 55 audit rows.
- Architecture test and fix for the Vite `/api/adminApi.ts` source-module/proxy collision.
- Auth spec behaviors: login, safe deep-link return and reload, real 2FA state, authenticated non-super forbidden state, unsafe-return fallback, and compact navigation.
- Governance behaviors: overview, security alerts, refresh, recent audit, and pagination.
- Organization spec skeleton, currently incomplete at its first form-label selector.

Task 9 evidence already obtained:

- `AdminViteDevProxyContractTest`: 1 passed, 7 assertions.
- Auth isolated receipt: four cases passed; mobile initially failed only on Arabic selector wording, then targeted mobile passed 1/1. Full auth spec still needs one final clean rerun.
- Governance: overview and security passed; pagination initially assumed the seeded last row was newest, was corrected to account for login activity, then targeted pagination passed 1/1. Full governance spec still needs one final clean rerun.
- Sanitized CSRF proof: `XSRF-TOKEN` and `mns-arad-session` are scoped to `127.0.0.1`, path `/`, `secure=false` locally, `SameSite=Lax`; login sends both cookies, `X-XSRF-TOKEN`, correct Origin and Referer. No CSRF bypass is used.

</completed_work>

<remaining_work>

### Finish Task 9

1. Inspect the interrupted organization screenshot/error context and replace the broad name-label selector with the exact rendered label/locator.
2. Complete organization create → persistence verification → update → protected delete → deterministic cleanup.
3. Add `admin-access.spec.ts` for canonical roles/governance/access summary, activity filtering/export, and incident settings, including persisted mutation or audit proof and cleanup.
4. Add `admin-users-departments.spec.ts` for canonical reads, persisted user/department workflows, cross-organization isolation, and cleanup.
5. Wire blocking Admin E2E into `.github/workflows/ci.yml` after PostgreSQL/Redis/build; no `continue-on-error`. Use port 5432 in CI while retaining local 5433 default.
6. Run all five specs separately from clean setup, then the full suite on the isolated local database.
7. Verify the development database on 5432 remains untouched; capture DB, cleanup, contract, Pint/typecheck, and exact browser receipts in `.superpowers/sdd/task-9-report.md`.
8. Commit only Task 9 paths, request a fresh read-only review, remediate all Critical/Important findings, rereview, then update the progress ledger.

### Task 10 — Operational application cutover

1. Read `.superpowers/sdd/task-10-brief.md` and re-read the live `resources/js/app.tsx` diff.
2. Write RED tests proving the operational app has no Admin shell/page imports, does not render `/admin/*`, and uses an absolute configured Admin origin.
3. Add a safe centralized URL helper driven by `VITE_ADMIN_URL`.
4. Remove only Admin imports/lazy declarations/routes/redirects from `resources/js/app.tsx`; preserve Meetings changes exactly.
5. Convert eligible Header, Sidebar, AppLayout, and NASAQ Admin entry points to external admin-origin links while retaining super-admin visibility gates.
6. Run focused routing/layout/NASAQ tests, typecheck, lint, zero-ownership searches, and diff checks.
7. Commit exact Task 10 paths and obtain a fresh independent review.

### Task 11 — Cleanup, full verification, and independent R3 audit

1. Prove old `resources/js/pages/admin/**` and `resources/js/widgets/admin-shell/**` have zero consumers before deleting them.
2. Remove only proven-dead compatibility frontend files/tests and only admin-only legacy aliases proven unused. Do not delete general operational APIs.
3. Verify Arabic/English complete-key parity and RTL against real resource strings.
4. Run the full matrix: Admin quality/build, operational quality, all five Admin E2E specs/full suite, targeted Laravel contract/isolation suites, Pint, PHPStan, Admin Docker build, Compose validation, manifest/source-map checks, and zero-ownership searches.
5. Map all 11 Definition of Done items to files and command receipts, including routing-only rollback/no schema diff.
6. Request a fresh independent read-only R3/Terra audit. Any failure returns to its owning task; do not self-approve a critical change.

</remaining_work>

<decisions_made>

- One repository, two independently built frontend applications, one Laravel backend, one PostgreSQL data model.
- Dedicated Admin origin with same-origin reverse proxy for `/api` and `/sanctum`.
- Literal `super_admin` is the global control-plane gate; `manage_organization` remains bounded and cannot admit users globally.
- Canonical `/api/admin/*` routes coexist temporarily with legacy routes and reuse the same controllers/requests.
- No database migration or duplicated data store is part of this extraction.
- Admin imports only its own tree plus explicitly allowed shared/entity modules; operational pages/widgets/app/features are forbidden.
- Local browser E2E uses file sessions, not the PHPUnit array session store.
- Local browser E2E database name/port are overridable to avoid destructive contention; CI retains isolated defaults.
- Old operational Admin pages are deleted only after cutover and zero-consumer proof.
- Rollback changes routing/link configuration, not persisted data.

</decisions_made>

<blockers>

- Shared test DB contention: mitigated locally with existing `iradah_pmo_admin_e2e_test` on `postgres-test:5433`.
- Dirty shared tree: unrelated Meetings changes and new files are user-owned. They are not part of Admin work and must never be staged by broad commands.
- Interrupted organization spec: not a product defect yet; current failure is an unresolved browser locator and needs exact inspection.
- No human approval, credential, or external system action is currently required to finish the local implementation and verification.

</blockers>

## Required Reading (in order)

1. `.planning/.continue-here.md` — this complete state and safety contract.
2. `.planning/HANDOFF.json` — machine-readable task and file state.
3. `AGENTS.md` — repository routing, safety, verification, and shared-tree rules.
4. `docs/superpowers/specs/2026-07-11-independent-admin-application-design.md` — approved architecture and Definition of Done.
5. `docs/superpowers/plans/2026-07-11-independent-admin-application.md` — exact Tasks 1–11 and verification matrix.
6. `.superpowers/sdd/progress.md` — authoritative completed-task ledger.
7. `.superpowers/sdd/task-9-brief.md` — current task contract.
8. `playwright.admin.config.ts`, `database/seeders/AdminE2ETestSeeder.php`, `e2e/admin/**`, `package.json`, `vite.admin.config.ts`, and `tests/Architecture/AdminViteDevProxyContractTest.php` — unstaged Task 9 implementation.
9. `.superpowers/sdd/task-10-brief.md` — serialized cutover instructions after Task 9 approval.

## Infrastructure State

- PostgreSQL development: port 5432; must never be migrated or wiped by tests.
- PostgreSQL test service: `erada-platform-postgres-test`, host port 5433, healthy at pause time.
- Local isolated Admin E2E DB: `iradah_pmo_admin_e2e_test`, exists and currently has the deterministic Admin fixture.
- Default CI/test DB: `iradah_pmo_test`; only safe when the job/process owns it exclusively.
- Redis/dev data services were healthy during Task 9 setup.
- Admin E2E frontend port: 4174; no listener remains.
- Laravel E2E backend port: 8000; no listener remains.
- No production deploy or external traffic cutover has been performed.

## Uncommitted Admin Task 9 Files

- Modified: `package.json`
- Modified: `vite.admin.config.ts`
- New: `playwright.admin.config.ts`
- New: `database/seeders/AdminE2ETestSeeder.php`
- New: `tests/Architecture/AdminViteDevProxyContractTest.php`
- New: `e2e/admin/helpers/admin-auth.ts`
- New: `e2e/admin/admin-auth.spec.ts`
- New: `e2e/admin/admin-governance.spec.ts`
- New/incomplete: `e2e/admin/admin-organizations.spec.ts`

Not yet created/modified for Task 9: `e2e/admin/admin-access.spec.ts`, `e2e/admin/admin-users-departments.spec.ts`, `.github/workflows/ci.yml`, `.superpowers/sdd/task-9-report.md`.

## Final Verification Commands

Run targeted commands throughout; before declaring completion, execute at minimum:

```bash
npm run admin:quality
npm run quality
ADMIN_E2E_DB_DATABASE=iradah_pmo_admin_e2e_test npm run test:e2e:admin
php artisan test tests/Feature/Api/Core/SuperAdminDashboardControllerTest.php tests/Feature/Api/Admin/AdminRouteContractTest.php tests/Feature/Api/Core/OrganizationControllerTest.php tests/Feature/Api/RoleControllerTest.php tests/Feature/Core/GovernanceRulesApiTest.php tests/Feature/Api/Core/ScopeTypeControllerTest.php tests/Feature/Security/ActivityLogIsolationTest.php tests/Feature/Core/UserIndexIsolationTest.php tests/Feature/Core/UserShowIsolationTest.php tests/Feature/Core/UserUpdateIsolationTest.php tests/Feature/Api/DepartmentOrganizationScopeTest.php tests/Architecture/AdminDeploymentContractTest.php tests/Architecture/AdminViteDevProxyContractTest.php
./vendor/bin/pint --test
composer phpstan
docker build -f Dockerfile.admin -t erada-admin:verification .
docker compose config --quiet
node scripts/check-admin-boundaries.mjs
rg -n 'pages/admin|admin-shell|path="/admin' resources/js/app.tsx
```

For any full-suite flake, retain the full failure receipt and rerun the exact failing class in isolation. Never dismiss a targeted failure as a flake.

<context>

The execution strategy is deliberately serial because Task 9 owns browser/database setup, Task 10 touches the dirty operational router, and Task 11 deletes compatibility code. Each task gets a bounded implementer, root verification, an exact-path commit, and a fresh read-only reviewer. Critical/Important findings must be fixed and rereviewed before advancing.

The immediate technical state is healthy: the formerly confusing CSRF 419 is fully diagnosed and fixed without a bypass. The remaining work is breadth and cutover proof, not a known architecture redesign.

</context>

<next_action>

Start with: acknowledge the four blocking constraints, verify `git status` and `git diff -- resources/js/app.tsx`, read the interrupted organization Playwright artifact/error context, then finish `e2e/admin/admin-organizations.spec.ts` against `ADMIN_E2E_DB_DATABASE=iradah_pmo_admin_e2e_test` before creating the remaining two specs.

</next_action>
